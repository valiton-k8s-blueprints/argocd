acme:
  url: https://acme-v02.api.letsencrypt.org/directory
  dns01:
    enabled: true
    solver:
      webhook:
        groupName: acme.stackit.de
        solverName: stackit
        config:
          authTokenSecretNamespace: cert-manager
          projectId: test-123-project
